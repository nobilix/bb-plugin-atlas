{
  "meta": {
    "bbVersion": "0.43.3",
    "commit": "e865697f56bea89f3413dd4cc7fae964850d20a0",
    "commitDate": "2026-09-18",
    "permalinkBase": "https://github.com/get-bb/bb/blob/e865697f56bea89f3413dd4cc7fae964850d20a0/",
    "repo": "get-bb/bb",
    "sdkVersion": "0.4.104",
    "tag": "desktop-v0.43.3",
    "url": "https://github.com/get-bb/bb"
  },
  "rules": [
    {
      "id": "access-acquire-idempotent",
      "text": "`acquire` is idempotent by key and must persist the revocation identity before returning, so `release` works even before enrolment completes."
    },
    {
      "id": "access-release-null-grant",
      "text": "`release` receives a null `grantId` when acquisition was interrupted — reconcile by key and hostId."
    },
    {
      "id": "ai-failures-returned",
      "text": "Return failures as `{ ok: false, code }` instead of throwing, so core can apply its retry and fallback policy (timeouts and rate limits fall back; auth failures do not)."
    },
    {
      "id": "ai-needs-host",
      "text": "An AI service must declare a `bb.host` entry; registering without one fails the load, and core always calls the primary host."
    },
    {
      "id": "ai-reserved-ids",
      "text": "Ids the server serves itself (`openai` transcription and the built-in inference providers) are reserved and refused."
    },
    {
      "id": "backend-full-trust",
      "text": "Plugins are full-trust code inside the server process: they can read all local bb data, including other plugins' secrets. `secret: true` settings are plaintext files with mode 0600, not encrypted. Design accordingly."
    },
    {
      "id": "bridge-grammar-v3",
      "text": "The delta assembler speaks grammar v3 only. Every turn needs `input.accepted` → `turn.open` → deltas → `turn.boundary`, even a zero-work prompt."
    },
    {
      "id": "browser-allow-personal",
      "text": "Pass `allowPersonal: true` only when the user explicitly picked an existing tab, and close only the tabs your plugin created."
    },
    {
      "id": "browser-host-match",
      "text": "Treat a connection whose `hostId` differs from your scope as fatal — the CDP endpoint is host-scoped."
    },
    {
      "id": "browser-lease-ttl",
      "text": "Control leases really expire: clamp your session's own expiry to the minimum of your TTL, the lease's and the connection's."
    },
    {
      "id": "browser-page-null",
      "text": "`experimental_page` is null outside the desktop app, and it takes no CDP control lease — no control banner appears. The `isolated` world shares the DOM but not page globals."
    },
    {
      "id": "cli-argv",
      "text": "Core bb commands always win a name collision; your plugin stays reachable as `bb plugin run <id>`. Use `defineCli` so `--help`, unknown-flag hints and `--json` envelopes come for free."
    },
    {
      "id": "cli-output-cap",
      "text": "`PLUGIN_CLI_OUTPUT_MAX_BYTES` is 1 MiB of stdout + stderr combined, and the host rejects an oversized result atomically instead of clipping it."
    },
    {
      "id": "cli-runs-on-server",
      "text": "The CLI handler runs inside the server process, not in the CLI: `argv` excludes the command name and `ctx.signal` aborts when the invoking request disconnects."
    },
    {
      "id": "command-isavailable",
      "text": "`isAvailable` runs before listing and before keyboard invocation — keep it cheap and synchronous; errors are contained."
    },
    {
      "id": "command-shortcuts",
      "text": "A `defaultShortcut` is validated strictly: bare modifiers are rejected and Command/Control/Alt or an F1–F24 key is required. User overrides live under `plugin:<plugin-id>/<command-id>`."
    },
    {
      "id": "composer-inline-cap",
      "text": "bb keeps at most three plugin inline actions in the control row, ranked by use; the rest move into an overflow menu."
    },
    {
      "id": "composer-scoped-effects",
      "text": "`setTextEffect` and `setInputLock` are scoped to your plugin and auto-release when your slot unmounts or the composer scope changes."
    },
    {
      "id": "composer-scopes",
      "text": "Declare `scopes` deliberately: `thread`, `queued-message`, `side-chat` and `new-thread` behave differently, and some (queued-message, side chat) have no submit pipeline at all."
    },
    {
      "id": "composer-submit-pipeline",
      "text": "Submit through `composer.experimental_submit(...)`, not your own request: the draft's attachments, @-mentions and (in new-thread) provider/model/reasoning/tier/permission/environment selections only travel that way."
    },
    {
      "id": "composer-write-target",
      "text": "Writes land in the composer that mounted your slot: the queued-message editor, the visible side-chat draft, the thread draft, or otherwise the new-thread draft — which persists until the user sends or clears it."
    },
    {
      "id": "content-script-trust",
      "text": "A content script is trusted, same-origin page code — not a sandbox. It runs once per window, tab and remote client, and everything it touches must be released through the abort signal or the returned disposer."
    },
    {
      "id": "crash-boundary",
      "text": "Each slot is wrapped in its own error boundary. A crash shows a chip and the rest of bb keeps working — but the crash is sticky for the session, so fix it rather than relying on the fallback."
    },
    {
      "id": "css-scope",
      "text": "Your styles are scoped to `:where([data-bb-plugin=<id>],[data-bb-plugin-root]:not([data-bb-plugin]))`. Only default-theme Tailwind utilities are emitted; custom `@theme` colors and hand-written `oklch(...)` literals will not survive the build."
    },
    {
      "id": "diff-patch-complete",
      "text": "`patch` is always a complete single-file unified patch whatever the caller supplied. Verify paths and hunk lines agree before treating `experimental_fullFileContents` as complete."
    },
    {
      "id": "directive-attrs-untrusted",
      "text": "Directive attributes are untrusted strings parsed out of the message. Validate your own fields; anything malformed falls back to rendering the original source text."
    },
    {
      "id": "dispose-lifo",
      "text": "The factory is time-boxed at 30 s and its return value is discarded. Register cleanup with `bb.onDispose` (LIFO) — services, schedules, listeners, content scripts, everything."
    },
    {
      "id": "engines-gate",
      "text": "Declare the floor you rely on in `engines.bbPluginSdk` (`app.commands.register` needs `>=0.4.91`, keyboard bindings `>=0.4.92`). Managed installs refuse a plugin whose SDK is newer than the host."
    },
    {
      "id": "env-create-idempotent",
      "text": "`create` is one long call and must be idempotent for its `pathKey`: after a restart core calls it again with the same attempt and pathKey."
    },
    {
      "id": "env-failure-terminal",
      "text": "A failed create is terminal — there is no automatic retry ladder. Return `{ status: \"failed\", message }` with user-safe recovery copy."
    },
    {
      "id": "env-hooks-owned-by-core",
      "text": "Core runs `.bb-env-setup.sh` after a create that returns `ownsPath: true` and `.bb-env-teardown.sh` before remove. Providers must not call these hooks themselves."
    },
    {
      "id": "env-inputs-public",
      "text": "Parsed `inputs` are persisted and readable by every plugin, including after the environment is destroyed. They are configuration, not a credential store."
    },
    {
      "id": "events-are-announcements",
      "text": "Events are announcements: the return value is ignored and a handler can never block or veto the change. The surface that can decide is `bb.experimental_hooks`."
    },
    {
      "id": "events-see-everything",
      "text": "Every listener sees every row, including other plugins'. Filter on `entry.waitingOn?.kind === \"plugin\" && entry.waitingOn.pluginId === bb.pluginId`."
    },
    {
      "id": "exclusive-slot",
      "text": "Only one plugin can own this at a time; the user picks the winner in Settings → Appearance. Do not assume yours is active."
    },
    {
      "id": "experimental-churn",
      "text": "Every `experimental_` member is in `docs/api_to_audit.md` and gets renamed project-wide when it stabilizes. Compatibility aliases warn for one release and then throw."
    },
    {
      "id": "file-source-kinds",
      "text": "`path` semantics follow `source.kind`: worktree-relative for `workspace`, absolute for `host`, storage-relative for `thread-storage`."
    },
    {
      "id": "frontend-no-network",
      "text": "The frontend has no network plane of its own. Call your server entry through `useRpc` → `bb.rpc`; never `fetch` from `app.tsx`."
    },
    {
      "id": "harness-same-collector",
      "text": "`loadPluginApp` runs the same validating collector as the host with the same error messages — pass the thunk form `() => import(\"./app\")` so the module evaluates after the runtime is installed."
    },
    {
      "id": "harness-stubs",
      "text": "Host components in the frontend harness are prop-recording stubs, not the real engines, and the harness composer has no submit pipeline."
    },
    {
      "id": "header-geometry",
      "text": "The thread header is a 48 px chrome row with 28 px controls: render one inline control and portal anything taller."
    },
    {
      "id": "hook-fail-closed",
      "text": "The dispatch hook is fail-closed: a handler that throws or exceeds the 10 s decision box fails the attempt with your plugin named. Decide in milliseconds."
    },
    {
      "id": "hook-idempotent",
      "text": "The pass re-runs on every drain, restart and retry, so a handler must be idempotent for one logical dispatch."
    },
    {
      "id": "hook-no-amendment",
      "text": "There is no \"handled it myself\" answer and no amendment arm: a hook decides (proceed / wait / reject), it never rewrites the dispatch."
    },
    {
      "id": "host-call-not-in-factory",
      "text": "Create the client and register signal handlers in the factory, but call host methods only from an RPC/event handler, background service or timer — candidate-time calls are rejected."
    },
    {
      "id": "host-limits",
      "text": "Host RPC limits: 32 MiB in, 8 MiB out, 30 s default timeout capped at 30 minutes, worker start 10 s, idle eviction after 5 minutes unless retained."
    },
    {
      "id": "host-no-private-imports",
      "text": "A host artifact may not import bb's private `@bb/*` packages at all, and it is fully self-contained: pure-JS dependencies are bundled into `dist/host.js`."
    },
    {
      "id": "host-worker-exit",
      "text": "`experimental_onWorkerExit` fires only on unexpected exits: graceful reload, disable, uninstall, daemon shutdown and idle eviction are silent."
    },
    {
      "id": "http-exact-match",
      "text": "HTTP routes are exact-match under `/api/v1/plugins/<id>/http`: `:` and `*` are literal characters. `auth: \"none\"` is only for webhooks that verify their own signature."
    },
    {
      "id": "icons-hugeicons",
      "text": "Icon names are bb's own set (hugeicons), not lucide. A plugin-owned glyph is declared in `bb.branding.experimental_icons` and referenced as `\"<pluginId>/<name>\"`."
    },
    {
      "id": "instructions-cap",
      "text": "Tool `instructions` and every instruction contribution are capped at 4096 characters; `configure` selections are capped at 256 ids."
    },
    {
      "id": "interaction-detached",
      "text": "`bb.ui.requestInput` survives the end of the agent's turn: the answer arrives later as a system message. Handle `cancelled` with all of its reasons (user, request-aborted, thread-stopped, thread-deleted, plugin-disposed, server-restarted, timeout)."
    },
    {
      "id": "interaction-limits",
      "text": "Hard limits: title 1–160 chars, payload ≤64 KiB of JSON, `timeoutMs` default 10 min and capped at 1 hour. bb stores neither the payload nor the submitted value — only what `describeSubmission` returns."
    },
    {
      "id": "jsx-alias-experimental",
      "text": "JSX reads a lowercase-initial name as an intrinsic element, so import `experimental_*` components under an aliased capitalised name (`experimental_Diff as Diff`)."
    },
    {
      "id": "kv-value-cap",
      "text": "`bb.storage.kv` values are JSON capped at 256 KB each; anything bigger or relational belongs in `bb.storage.database()`."
    },
    {
      "id": "machine-checkpoint",
      "text": "`await checkpoint(resource)` after durable allocation and before bootstrap, and never put the bootstrap bundle in the resource JSON."
    },
    {
      "id": "machine-idle-is-yours",
      "text": "Idle timing is the plugin's, not core's: watch activity events and sweep with a schedule plus startup reconciliation."
    },
    {
      "id": "machine-needs-composition",
      "text": "Registering a machine provider contributes no environment-picker entry: also register a composition naming `machineProviderId` + `environmentProviderId`."
    },
    {
      "id": "machine-resource-public",
      "text": "`inputs`, `resource` and every `report.step` / `report.log` line are persisted and readable by other plugins and copied into thread transcripts. Store references, never secrets."
    },
    {
      "id": "mention-images-untrusted",
      "text": "Treat remote and page-derived image content attached through `experimental_images` as untrusted evidence."
    },
    {
      "id": "mention-resolve-blocks",
      "text": "`resolve` runs once per unique item at send time and a throw blocks the send with a visible error. Item ids are namespaced by the host as `\"<providerId>:<itemId>\"`."
    },
    {
      "id": "mention-search-timebox",
      "text": "`search` runs on the server, is time-boxed to 2 s and is failure-isolated: a slow or throwing provider simply contributes nothing."
    },
    {
      "id": "message-cancelled-only-signal",
      "text": "`message.cancelled` is the only signal that a queued row was removed before it ever dispatched."
    },
    {
      "id": "message-reference",
      "text": "`ThreadChatMessageReference` is a narrow stable reference, never an internal timeline row; `sourceSeqEnd` is the anchor the server accepts for provider-history forks."
    },
    {
      "id": "metadata-frozen-in-configure",
      "text": "Inside `bb.agents.configure` the namespace arrives deep-frozen — copy it before handing values to anything that expects mutable `JsonValue`."
    },
    {
      "id": "metadata-size",
      "text": "Each namespace caps at 256 KiB of UTF-8 JSON; an over-size patch fails with HTTP 413 and leaves the namespace unchanged."
    },
    {
      "id": "metadata-untrusted",
      "text": "Namespace ids are not an ownership or authorization boundary: any API client, another plugin or the thread's own agent can write any namespace. Treat values as untrusted input."
    },
    {
      "id": "migrations-append-only",
      "text": "Migrations are append-only by index: the host records each statement's hash and rejects a changed or reused index."
    },
    {
      "id": "mount-timeout",
      "text": "Async mount setup is time-boxed at 10 s. A throwing or slow mount aborts the candidate generation, disposes what already mounted in reverse and publishes nothing."
    },
    {
      "id": "needs-configuration-error",
      "text": "`NeedsConfigurationError` is matched by name — `throw Object.assign(new Error(msg), { name: \"NeedsConfigurationError\" })` — so a service can mark the plugin instead of crash-looping."
    },
    {
      "id": "no-worker-pool",
      "text": "`experimental_Diff` takes the host's shared highlight worker pool from React context: thread and nav panels have one, homepage and settings sections do not, so code there renders unhighlighted."
    },
    {
      "id": "overlay-owns-chrome",
      "text": "bb supplies no chrome, positioning or visibility for an overlay: it mounts once per app window and you own everything it draws."
    },
    {
      "id": "panel-params-untrusted",
      "text": "Panel `params` round-trip through persistence, so treat them as untrusted input: re-fetch fresh data by id instead of embedding whole payloads."
    },
    {
      "id": "provider-derive-sync",
      "text": "`deriveProviderOptions` is synchronous, runs on every session and turn command and is capped at 64 KiB; a throw fails the command with your plugin named."
    },
    {
      "id": "provider-id-immutable",
      "text": "A provider `id` is persisted on every thread that used it — never change it. Ids are flat and collision-rejected across plugins."
    },
    {
      "id": "provider-needs-host",
      "text": "Registering a provider without a `bb.host` entry fails the plugin load: the bridge ships in the host artifact."
    },
    {
      "id": "realtime-broadcast",
      "text": "`bb.realtime.publish` broadcasts to every connected client and nothing is persisted or replayed — it is not a confidentiality boundary, and durable state must be reconciled on later `connected` transitions."
    },
    {
      "id": "replacement-original",
      "text": "bb passes `Original`. Render it to delegate — that is the supported way to handle the cases you do not want to own."
    },
    {
      "id": "richtext-paint-only",
      "text": "Rich-text effects are paint-only: they never mutate the draft, and offsets are plain-text offsets into the current structured draft."
    },
    {
      "id": "rpc-strict-json",
      "text": "RPC results must be strict JSON: cycles, bigint, `undefined`, functions, class instances, symbol keys and non-finite numbers are rejected, not coerced."
    },
    {
      "id": "schedule-only-while-loaded",
      "text": "A cron schedule fires only while the plugin is loaded, so anything that must survive a disable needs its own startup reconciliation."
    },
    {
      "id": "sdk-attribution",
      "text": "`threads.spawn` / `threads.fork` default `origin` to `\"plugin\"` and `originPluginId` to your id; seeding `pluginMetadata` always attributes the new thread to you."
    },
    {
      "id": "sdk-bind-gated",
      "text": "`bb.sdk` is bind-gated: reading it before the host binds it throws. Prefer using it from handlers, services and timers so isolated harnesses behave the same."
    },
    {
      "id": "sdk-hidden-threads",
      "text": "Hidden worker threads need explicit cleanup — stop and archive them in a `finally` so a plugin failure cannot retain the agent process."
    },
    {
      "id": "secrets-agent-reach",
      "text": "An agent shell running as the same user on the same machine can read your plugin's data and secrets, and the local API is unauthenticated. If a capability must not be triggered by the agent alone, split it: the agent proposes, a human confirms through `bb.ui.requestInput`."
    },
    {
      "id": "send-now-bypass",
      "text": "A user's explicit Send-now bypasses the pass by design, as do compaction and an edited message's re-send."
    },
    {
      "id": "service-abort-sleep",
      "text": "A service must resolve when its `signal` aborts — a plain `setTimeout` sleeps through the stop window and the plugin reports \"degraded (service did not stop)\" on reload."
    },
    {
      "id": "settings-load-once",
      "text": "Settings are read once per load. Saving does not reload the plugin in V1 — tell the user to run `bb plugin reload <id>`, or subscribe with `onChange`."
    },
    {
      "id": "settings-secrets",
      "text": "Only string settings can be `secret: true`; secrets are never sent to the frontend (`useSettings()` sees non-secret values only) and never stored in the database."
    },
    {
      "id": "shims-devdeps",
      "text": "React, the SDK and every shimmed package (sonner, vaul, radix, @pierre/diffs, clsx, tailwind-merge, cva) come from the host at runtime. They belong in `devDependencies` — putting one in `dependencies` bundles a second copy and breaks the plugin."
    },
    {
      "id": "split-instances",
      "text": "A split layout mounts one instance per visible pane. Never keep per-thread state in a module-level singleton."
    },
    {
      "id": "status-cleared-on-load",
      "text": "`needsConfiguration` is cleared on the next load, so re-report it from the factory on every load while the configuration is still missing."
    },
    {
      "id": "storage-not-a-boundary",
      "text": "Storage isolation is a convention of the `bb.storage` helpers, not an enforced boundary — another plugin can open your `data.db` with ordinary `fs`."
    },
    {
      "id": "thread-list-keyboard",
      "text": "Row anchors must carry `data-sidebar-thread-shortcut-target=\"\"` and `data-sidebar-thread-id={thread.id}`, or bb's `thread.next` / `thread.previous` shortcuts silently stop working."
    },
    {
      "id": "threadchat-owns-thread",
      "text": "`ThreadChat` owns timeline loading, streaming, drafts, send/queue/steer/stop, attachments and pending interactions — do not proxy thread data through your own RPC."
    },
    {
      "id": "timeline-kind-ownership",
      "text": "You may only render your own extension kinds (`\"<pluginId>/<name>\"`) and `\"tool\"` rows of providers your plugin registered. Core kinds always use core renderers."
    },
    {
      "id": "tool-name-global",
      "text": "Tool names are unique across plugins. A collision is not fatal: the later tool is dropped and the reason lands in your plugin's status detail."
    },
    {
      "id": "tool-output-bounded",
      "text": "Return bounded output — `PLUGIN_CLI_OUTPUT_MAX_BYTES` caps CLI output and oversized tool results get truncated."
    },
    {
      "id": "tools-next-session",
      "text": "A changed tool set or changed instructions take effect only when the provider session is next started or resumed — bb never hot-mutates a running session."
    }
  ]
}
