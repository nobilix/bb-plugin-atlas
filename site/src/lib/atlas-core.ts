/*
 * The site's single door to `packages/atlas-core`.
 *
 * The brief a reader copies from a reference page, the brief `/briefs.json`
 * serves and the brief the zone map puts on the clipboard are one function
 * called three times: the brief generator lives once.
 */
export * from '@atlas/core';
