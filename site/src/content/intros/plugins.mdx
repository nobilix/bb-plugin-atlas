---
title: "Catalog: first-party plugins and examples"
description: Every first-party plugin and example at the pinned release, with its entries and size.
---

Every first-party plugin and example at the pinned release, with the entries it declares and its size. To find the plugin that shows a specific surface working, start from [Choosing a surface](/choose/), which maps goals to plugins. The Entries column reads S = `bb.server`, A = `bb.app`, H = `bb.host`. LOC is `wc -l` over `.ts`/`.tsx` excluding tests, `dist/` and `node_modules/`; the figures are for comparison, not exact.
