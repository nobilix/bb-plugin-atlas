# The public address: the origin, plus the path when the site is served from a
# subdirectory. Used for the absolute URLs in llms.txt and the sitemap, and as
# Astro's `base` for every internal link.
SITE_URL=http://localhost:4321
